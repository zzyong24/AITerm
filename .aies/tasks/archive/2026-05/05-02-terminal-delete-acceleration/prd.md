# PRD: 终端删除键加速

## 背景

用户在终端输入长命令时，需要频繁按住 Backspace 逐字符删除，效率低。
浏览器原生 key repeat 速度偏慢，不如系统终端体验流畅。

## 目标

在 AITerm 终端组件中，当用户长按 Backspace 时，实现加速删除效果，
使删除速度随长按时长逐渐加快，接近系统终端体验。

## 实现范围

- 仅影响终端输入区（`xterm.js` 所在区域）
- 不影响编辑器或其他输入框
- 加速策略：初始正常速率，持续按住后切换至快速删词模式
