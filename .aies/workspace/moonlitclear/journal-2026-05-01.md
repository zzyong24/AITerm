# 工作日志 2026-05-01

## 完成的 work

### 1. AIES 脚手架初始化 ✅
- 填充 `.ai/index.md` 项目索引
- 填充 `.aies/spec/architecture.md` 架构规范
- 填充 `.aies/spec/code-style.md` 代码风格
- 填充 `.aies/spec/testing.md` 测试规范

### 2. 终端滚动卡顿修复 ✅
- 安装 `@xterm/addon-webgl` 包
- 修改 `Terminal.vue`，加载 WebGL 渲染器

### 3. Mermaid 预览 — 完成 ✅
- **第一次尝试失败**：CodeMirror Decoration.replace 内联渲染（`Decoration.replace` 是行内替换，无法做块级替换）
- **第二次尝试成功**：编辑/预览切换模式
  - 仅 `.md` 文件显示预览按钮
  - 预览模式用 `marked` 解析 markdown，mermaid 代码块渲染为 SVG
  - 类似 GitHub 的 markdown 样式

## 踩坑记录

| 坑 | 原因 | 教训 |
|---|---|---|
| ratio-terminal 不存在 | 以为是 npm 包，实际搜索不到 | Web search 结果需验证 npm registry |
| Mermaid 无法在 Node.js 渲染 | Mermaid 依赖 DOM (d3-selection) | 测试渲染需浏览器环境 |
| Decoration.replace 无法做块级替换 | CodeMirror 设计是行内替换 | 实现 Obsidian 效果需要不同方案 |

## 技术方案决策

**Mermaid 渲染最终选择**：编辑/预览切换模式
- 放弃内联渲染（CodeMirror 限制太多）
- 改用 Typora/GitHub 风格：编辑模式编辑，预览模式渲染
- 工具栏添加眼睛图标按钮，仅 markdown 文件显示

## 2026-05-01 22:31:33 — Electron 客户端暴露 HTTP 网页端

**Commit**: `a07602c`

**摘要**: Electron main process 内嵌 Express HTTP 服务器（端口 5003），serve dist/ 静态文件；打包后 BrowserWindow 加载 http://localhost:5003；三端可通过 Tailscale 互通。修改文件：electron/main.ts（新增内嵌服务器）、package.json（asarUnpack dist）；新增 e2e/electron-web-server.spec.ts


## 2026-05-04 14:00:56 — 修复浏览器白屏

**摘要**: 将 BrowserView.vue 的 <iframe> 换成 Electron <webview>，修复 X-Frame-Options 导致的白屏。改了两个文件：electron/main.ts 加 webviewTag:true，BrowserView.vue 替换渲染方式并用 setZoomFactor() 做缩放。新增 spec/guides/browser-embedding.md。


## 2026-05-04 20:04:06 — Server-as-SSOT 重构完成 + AIES 合规收尾

**摘要**: 完成 Server-as-SSOT 架构重构：PtyService 广播 sessions_snapshot，AppBusiness 新增 onSessionsSnapshot/rebuildTabsFromSessions 原子替换，closeSession 改为乐观 UI。单元测试 27/27，E2E 9passed+1skipped。修复 task.json 缺少 title 字段问题，创建 acceptance.md / context.jsonl，完成 Phase 3 Spec 回流：testing.md 新增 vitest node 环境 + playwright skip 模式 + native rebuild 三条，cross-layer.md 重写为 AITerm TypeScript SSOT 规范，首次创建 .ai/index.md + changelog.md。

